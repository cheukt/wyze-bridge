# Copy to .env.viam and fill in your machine part id.
# Find it in the Viam app under your machine's part → "Copy part ID".
# Used by `make reload` for cloud hot-reload of the module.
VIAM_PART_ID=your-part-id
